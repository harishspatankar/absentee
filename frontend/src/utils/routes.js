const root = '/';
const dashboard = '/dashboard';
const routes = {
  root,
  dashboard,
  teachers: '/teachers',
  addTeachers: '/teachers/add',
  editTeacher: '/teachers/:id/edit',
};

export default routes;
